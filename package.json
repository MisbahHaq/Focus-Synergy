{
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:env": "node scripts/build-frontend.js",
    "static-server": "node scripts/static-server.js",
    "deploy:rules": "firebase deploy --only firestore:rules"
  },
  "dependencies": {
    "firebase": "^12.13.0",
    "ico": "^0.3.3",
    "png-to-ico": "^3.0.2",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0"
  }
}
